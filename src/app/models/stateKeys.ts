import { makeStateKey } from '@angular/platform-browser';

export const STATE_KEY_MENUS = makeStateKey('menus');
export const STATE_KEY_CATALOG_ITEMS = makeStateKey('catalog-items');
export const STATE_KEY_FEATURED_ITEMS = makeStateKey('featured-items');
export const STATE_KEY_RECOMMENED_ITEMS = makeStateKey('recommended-items');
export const STATE_ORDERS_HISTORY = makeStateKey('orders-history');
